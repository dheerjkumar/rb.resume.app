require('dotenv').config(); const jwt = require('jsonwebtoken'); const token = jwt.sign({ id: '6aa0dc265b7ed9138fe09186' }, process.env.JWT_SECRET); fetch('http://localhost:5000/api/users/tour', { method: 'PATCH', headers: { cookie: 'token=' + token } }).then(r => r.json()).then(console.log).catch(console.error);
